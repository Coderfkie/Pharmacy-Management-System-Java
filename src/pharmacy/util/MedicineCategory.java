package pharmacy.util;
/**
 * this class use Enum to represent medicine categories
 * we use ENUM usage
 */
public enum MedicineCategory {
    TABLET,
    SYRUP,
    INJECTION,
    OINTMENT
}
